#include "main.h"
#include <string.h>
/**
 * _strcat - concatenate two strings
 * @dest :a string input1
 * @src : a string input2
 *
 * Return: concatinated string
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
