#include "main.h"
/**
 * _strcpy - to copy string values to temp file
 * @dest: buffer pointer
 * @src: main pointer
 * Return: src arrays on temp from dest
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

}
