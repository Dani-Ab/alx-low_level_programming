#include "main.h"
/**
 * _abs:- checkes and return absolute value of the input number.
 *
 * @n: input character
 * Return: returns absolute value of a number
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
